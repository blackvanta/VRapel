(function() {
        // without jQuery (doesn't work in older IEs)
    document.addEventListener('DOMContentLoaded', function(){ 
        
        // let link_aboutUs = document.getElementById("")

    }, false);
 
 })();