(function() {

    function ready(){


    }

  // without jQuery (doesn't work in older IEs)
  document.addEventListener(
    "DOMContentLoaded",
    function() {
    //   let sectionItems = document.querySelectorAll(".section__gallery-item");
    //   sectionItems.forEach(e => {
    //     e.addEventListener("click", function(e) {
    //       console.log("this", this);
    //       console.log("e", e);
    //       if (this.classList.contains("full_screen"))
    //         this.classList.remove("full_screen");
    //       else this.classList.add("full_screen");
    //     });
    //   });
     ready()
    },
    false
  );
})();

